<!DOCTYPE html>
<html lang="en" data-theme="light">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= pageTitle %> - LMS EduManage</title>
  <link href="/css/output.css" rel="stylesheet">
  <link href="/css/custom.css" rel="stylesheet">
  <link href="/css/teacher-theme.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <!-- DataTables CSS -->
  <link rel="stylesheet" href="https://cdn.datatables.net/1.13.6/css/jquery.dataTables.min.css">
  <link rel="stylesheet" href="https://cdn.datatables.net/responsive/2.5.0/css/responsive.dataTables.min.css">
  <style>
    /* DataTables custom styling */
    .dataTables_wrapper .dataTables_length select,
    .dataTables_length select,
    select[name="assignmentsTable_length"] {
      padding: 0.5rem 2rem 0.5rem 0.75rem !important;
      border: 1px solid rgba(0, 0, 0, 0.1) !important;
      border-radius: 0.5rem !important;
      background-color: white !important;
      font-size: 0.875rem !important;
      min-height: 2.5rem !important;
      appearance: auto !important;
    }
    
    [data-theme="dark"] .dataTables_wrapper .dataTables_length select,
    [data-theme="dark"] .dataTables_length select,
    [data-theme="dark"] select[name="assignmentsTable_length"] {
      background-color: #374151 !important;
      border-color: rgba(75, 107, 251, 0.2) !important;
      color: #f3f4f6 !important;
    }
    
    /* DataTables responsive - full width on mobile when expanded */
    @media screen and (max-width: 1023px) {
      table.dataTable.dtr-inline.collapsed > tbody > tr > td.dtr-control,
      table.dataTable.dtr-inline.collapsed > tbody > tr > th.dtr-control {
        position: relative;
      }
      
      table.dataTable.dtr-inline.collapsed > tbody > tr.parent > td.child,
      table.dataTable > tbody > tr.child {
        padding: 1rem !important;
      }
      
      table.dataTable > tbody > tr.child ul.dtr-details {
        width: 100% !important;
      }
      
      table.dataTable > tbody > tr.child ul.dtr-details > li {
        border-bottom: none !important;
        padding: 0.75rem 0;
      }
    }
    
    .dataTables_wrapper .dataTables_filter input {
      padding: 0.5rem 0.75rem;
      border: 1px solid rgba(0, 0, 0, 0.1);
      border-radius: 0.5rem;
      background-color: white;
      font-size: 0.875rem;
      min-height: 2.5rem;
      margin-left: 0.5rem;
    }
    
    [data-theme="dark"] .dataTables_wrapper .dataTables_filter input {
      background-color: #374151;
      border-color: rgba(75, 107, 251, 0.2);
      color: #f3f4f6;
    }
    
    .dataTables_wrapper .dataTables_length,
    .dataTables_wrapper .dataTables_filter {
      font-size: 0.875rem;
      font-weight: 500;
    }
    
    .dataTables_wrapper .dataTables_length label,
    .dataTables_wrapper .dataTables_filter label {
      display: flex;
      align-items: center;
      gap: 0.5rem;
    }
    
    .dataTables_wrapper .dataTables_paginate .paginate_button {
      padding: 0.5rem 0.75rem;
      margin: 0 0.125rem;
      border-radius: 0.5rem;
      border: 1px solid rgba(0, 0, 0, 0.1);
      background-color: white;
      color: inherit;
    }
    
    .dataTables_wrapper .dataTables_paginate .paginate_button.current {
      background: linear-gradient(135deg, #4b6bfb 0%, #3b5bdb 100%);
      color: white;
      border-color: transparent;
    }
    
    .dataTables_wrapper .dataTables_paginate .paginate_button:hover {
      background: rgba(75, 107, 251, 0.1);
      border-color: rgba(75, 107, 251, 0.3);
      color: inherit;
    }
    
    [data-theme="dark"] .dataTables_wrapper .dataTables_paginate .paginate_button {
      background-color: #1f2937;
      border-color: rgba(75, 107, 251, 0.2);
    }
    
    [data-theme="dark"] .dataTables_wrapper .dataTables_paginate .paginate_button:hover {
      background-color: rgba(75, 107, 251, 0.15);
    }
  </style>
  <!-- jQuery (required for DataTables) -->
  <script src="https://code.jquery.com/jquery-3.7.0.min.js"></script>
  <!-- DataTables JS -->
  <script src="https://cdn.datatables.net/1.13.6/js/jquery.dataTables.min.js"></script>
  <script src="https://cdn.datatables.net/responsive/2.5.0/js/dataTables.responsive.min.js"></script>
</head>
<body>
  <%- include('../shared/navbar') %>

  <div class="container mx-auto px-4 py-4 sm:py-8">
    <!-- Page Header -->
    <div class="flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 mb-6 sm:mb-8">
      <div>
        <h1 class="text-2xl sm:text-3xl font-bold text-base-content mb-2">
          <i class="fas fa-clipboard-list mr-2"></i>All Assignments
        </h1>
        <p class="text-sm sm:text-base text-base-content/70">Manage assignments across all your courses</p>
      </div>
      <% if (courses.length > 0) { %>
        <details class="dropdown dropdown-end w-full sm:w-auto" id="createAssignmentDropdown">
          <summary class="btn-global-primary w-full sm:w-auto flex items-center justify-center cursor-pointer list-none">
            <i class="fas fa-plus mr-2"></i>Create Assignment
            <i class="fas fa-chevron-down ml-2"></i>
          </summary>
          <ul class="dropdown-content z-[1] menu p-2 shadow bg-base-100 rounded-box w-64 max-h-96 overflow-y-auto mt-2">
            <li class="menu-title"><span>Select Course</span></li>
            <% courses.forEach(course => { %>
              <li>
                <a href="/teacher/courses/<%= course.id %>/assignments/create">
                  <span class="badge badge-ghost badge-sm"><%= course.code %></span>
                  <%= course.title %>
                </a>
              </li>
            <% }); %>
          </ul>
        </details>
        <script>
            // Close dropdown when clicking outside
            document.addEventListener('click', function(event) {
                const dropdown = document.getElementById('createAssignmentDropdown');
                if (dropdown && dropdown.hasAttribute('open') && !dropdown.contains(event.target)) {
                    dropdown.removeAttribute('open');
                }
            });
        </script>
      <% } %>
    </div>

    <!-- Statistics Cards -->
    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4 sm:gap-6 mb-6 sm:mb-8">
      <!-- Total Assignments -->
      <div class="stats-card stat-card-gradient-primary">
        <div class="stat">
          <div class="stat-figure text-primary-content/50">
            <i class="fas fa-clipboard-list text-3xl sm:text-4xl" style="color: rgba(255,255,255,0.6);"></i>
          </div>
          <div class="stat-title text-white/80">Total Assignments</div>
          <div class="stat-value text-white"><%= stats.total %></div>
          <div class="stat-desc text-white/60">Across all courses</div>
        </div>
      </div>

      <!-- Active Assignments -->
      <div class="stats-card stat-card-gradient-success">
        <div class="stat">
          <div class="stat-figure text-white/50">
            <i class="fas fa-clock text-3xl sm:text-4xl"></i>
          </div>
          <div class="stat-title text-white/80">Active</div>
          <div class="stat-value"><%= stats.active %></div>
          <div class="stat-desc text-white/60">Deadline not passed</div>
        </div>
      </div>

      <!-- Overdue Assignments -->
      <div class="stats-card stat-card-gradient-error">
        <div class="stat">
          <div class="stat-figure text-white/50">
            <i class="fas fa-hourglass-end text-3xl sm:text-4xl"></i>
          </div>
          <div class="stat-title text-white/80">Overdue</div>
          <div class="stat-value"><%= stats.overdue %></div>
          <div class="stat-desc text-white/60">Deadline passed</div>
        </div>
      </div>

      <!-- Pending Grading -->
      <div class="stats-card stat-card-gradient-warning">
        <div class="stat">
          <div class="stat-figure text-gray-800/50">
            <i class="fas fa-pen text-3xl sm:text-4xl"></i>
          </div>
          <div class="stat-title text-gray-800/80">Pending Grading</div>
          <div class="stat-value"><%= stats.pendingGrading %></div>
          <div class="stat-desc text-gray-800/60">Submissions to grade</div>
        </div>
      </div>
    </div>

    <!-- Filters -->
    <div class="card bg-base-100 shadow-xl mb-6">
      <div class="card-body p-4 sm:py-4">
        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-3 sm:gap-4">
          <!-- Course Filter -->
          <div class="form-control w-full">
            <label class="label py-1">
              <span class="label-text font-semibold text-xs sm:text-sm"><i class="fas fa-book mr-1"></i>Filter by Course</span>
            </label>
            <select id="courseFilter" class="select select-bordered select-sm w-full">
              <option value="">All Courses</option>
              <% courses.forEach(course => { %>
                <option value="<%= course.id %>"><%= course.code %> - <%= course.title %></option>
              <% }); %>
            </select>
          </div>

          <!-- Status Filter -->
          <div class="form-control w-full">
            <label class="label py-1">
              <span class="label-text font-semibold text-xs sm:text-sm"><i class="fas fa-info-circle mr-1"></i>Status</span>
            </label>
            <select id="statusFilter" class="select select-bordered select-sm w-full">
              <option value="">All Status</option>
              <option value="active">Active</option>
              <option value="overdue">Overdue</option>
            </select>
          </div>

          <!-- Grading Status Filter -->
          <div class="form-control w-full">
            <label class="label py-1">
              <span class="label-text font-semibold text-xs sm:text-sm"><i class="fas fa-check-circle mr-1"></i>Grading</span>
            </label>
            <select id="gradingFilter" class="select select-bordered select-sm w-full">
              <option value="">All</option>
              <option value="pending">Has Pending</option>
              <option value="complete">All Graded</option>
            </select>
          </div>

          <!-- Reset Button -->
          <div class="form-control w-full">
            <label class="label py-1">
              <span class="label-text">&nbsp;</span>
            </label>
            <button onclick="resetFilters()" class="btn btn-ghost btn-sm w-full">
              <i class="fas fa-undo mr-1 sm:mr-2"></i>Reset
            </button>
          </div>
        </div>
      </div>
    </div>

    <!-- Assignments List -->
    <% if (assignments.length === 0) { %>
      <div class="card bg-base-100 shadow-xl">
        <div class="card-body text-center py-16">
          <i class="fas fa-clipboard-list text-6xl text-base-content/20 mb-4"></i>
          <h2 class="text-2xl font-bold mb-2">No Assignments Yet</h2>
          <p class="text-base-content/70 mb-6">Create your first assignment to get started</p>
          <% if (courses.length > 0) { %>
            <div class="flex justify-center">
              <a href="/teacher/courses/<%= courses[0].id %>/assignments/create" class="btn-global-primary btn-wide">
                <i class="fas fa-plus mr-2"></i>Create First Assignment
              </a>
            </div>
          <% } else { %>
            <div class="flex justify-center">
              <a href="/teacher/courses/create" class="btn-global-primary btn-wide">
                <i class="fas fa-book mr-2"></i>Create a Course First
              </a>
            </div>
          <% } %>
        </div>
      </div>
    <% } else { %>
      <!-- Desktop Table View (hidden on mobile) -->
      <div class="card bg-base-100 shadow-xl hidden lg:block">
        <div class="card-body p-2 sm:p-6">
          <div class="overflow-x-auto lg:overflow-x-visible -mx-2 sm:mx-0">
            <table id="assignmentsTable" class="table table-zebra w-full">
              <thead>
                <tr>
                  <th>Assignment</th>
                  <th>Course</th>
                  <th>Deadline</th>
                  <th>Status</th>
                  <th>Submissions</th>
                  <th>Grading</th>
                  <th>Actions</th>
                </tr>
              </thead>
              <tbody>
                <% assignments.forEach(assignment => { 
                  const isOverdue = new Date(assignment.deadline) < new Date();
                  const submissionCount = assignment.Submissions.length;
                  const gradedCount = assignment.Submissions.filter(s => s.marks !== null).length;
                  const pendingCount = submissionCount - gradedCount;
                  const materialCount = assignment.materials ? assignment.materials.length : 0;
                  const safeTitle = assignment.title.replace(/'/g, "\\'");
                %>
                  <tr data-course-id="<%= assignment.course.id %>" 
                      data-status="<%= isOverdue ? 'overdue' : 'active' %>"
                      data-pending="<%= pendingCount > 0 ? 'pending' : 'complete' %>">
                    <!-- Assignment Title & Description -->
                    <td>
                      <div class="flex items-start gap-3">
                        <div class="avatar placeholder flex-shrink-0">
                          <div class="<%= isOverdue ? 'stat-card-gradient-error' : 'stat-card-gradient-success' %> rounded-lg w-12 h-12 flex items-center justify-center text-white">
                            <i class="fas fa-clipboard-list text-xl"></i>
                          </div>
                        </div>
                        <div>
                          <div class="font-bold"><%= assignment.title %></div>
                          <div class="text-sm text-base-content/60 max-w-xs truncate">
                            <%= assignment.description || 'No description' %>
                          </div>
                          <% if (materialCount > 0) { %>
                            <div class="text-xs text-info mt-1">
                              <i class="fas fa-paperclip mr-1"></i><%= materialCount %> material<%= materialCount > 1 ? 's' : '' %>
                            </div>
                          <% } %>
                        </div>
                      </div>
                    </td>

                    <!-- Course -->
                    <td>
                      <a href="/teacher/courses/<%= assignment.course.id %>" class="link link-hover">
                        <div class="badge badge-ghost badge-lg">
                          <%= assignment.course.code %>
                        </div>
                        <div class="text-sm text-base-content/60"><%= assignment.course.title %></div>
                      </a>
                    </td>

                    <!-- Deadline -->
                    <td>
                      <div class="text-sm">
                        <%= new Date(assignment.deadline).toLocaleDateString('en-US', {
                          month: 'short',
                          day: 'numeric',
                          year: 'numeric'
                        }) %>
                      </div>
                      <div class="text-xs text-base-content/60">
                        <%= new Date(assignment.deadline).toLocaleTimeString('en-US', {
                          hour: '2-digit',
                          minute: '2-digit'
                        }) %>
                      </div>
                    </td>

                    <!-- Status -->
                    <td>
                      <% if (isOverdue) { %>
                        <span class="badge badge-error gap-1">
                          <i class="fas fa-times-circle"></i>Overdue
                        </span>
                      <% } else { %>
                        <span class="badge badge-success gap-1">
                          <i class="fas fa-clock"></i>Active
                        </span>
                      <% } %>
                    </td>

                    <!-- Submissions Count -->
                    <td>
                      <div class="flex items-center gap-2">
                        <i class="fas fa-file-alt text-info"></i>
                        <span class="font-semibold"><%= submissionCount %></span>
                      </div>
                    </td>

                    <!-- Grading Progress -->
                    <td>
                      <% if (submissionCount === 0) { %>
                        <span class="text-base-content/50 text-sm">No submissions</span>
                      <% } else if (pendingCount === 0) { %>
                        <span class="badge badge-success gap-1">
                          <i class="fas fa-check"></i>All graded
                        </span>
                      <% } else { %>
                        <div class="flex items-center gap-2">
                          <progress class="progress progress-warning w-16" value="<%= gradedCount %>" max="<%= submissionCount %>"></progress>
                          <span class="text-xs"><%= gradedCount %>/<%= submissionCount %></span>
                        </div>
                        <div class="text-xs text-warning mt-1">
                          <i class="fas fa-hourglass-half mr-1"></i><%= pendingCount %> pending
                        </div>
                      <% } %>
                    </td>

                    <!-- Actions -->
                    <td>
                      <div class="flex flex-row gap-1">
                        <a href="/teacher/assignments/<%= assignment.id %>/submissions" 
                           class="btn-global-primary btn-xs sm:btn-sm" title="View Submissions">
                          <i class="fas fa-eye text-xs"></i>
                        </a>
                        <a href="/teacher/assignments/<%= assignment.id %>/edit" 
                           class="btn-glass-custom btn-xs sm:btn-sm flex items-center justify-center" title="Edit Assignment">
                          <i class="fas fa-edit text-xs"></i>
                        </a>
                        <button onclick="deleteAssignment('<%= assignment.id %>', '<%= safeTitle %>')" 
                                class="btn-global-danger btn-xs sm:btn-sm" title="Delete Assignment">
                          <i class="fas fa-trash text-xs"></i>
                        </button>
                      </div>
                    </td>
                  </tr>
                <% }); %>
              </tbody>
            </table>
          </div>
        </div>
      </div>

      <!-- Mobile Card View (visible on mobile only) -->
      <div class="lg:hidden space-y-3">
        <% assignments.forEach((assignment, idx) => { 
          const isOverdue = new Date(assignment.deadline) < new Date();
          const submissionCount = assignment.Submissions.length;
          const gradedCount = assignment.Submissions.filter(s => s.marks !== null).length;
          const pendingCount = submissionCount - gradedCount;
          const materialCount = assignment.materials ? assignment.materials.length : 0;
          const safeTitle = assignment.title.replace(/'/g, "\\'");
        %>
          <div class="collapse collapse-arrow bg-base-100 shadow-lg" data-course-id="<%= assignment.course.id %>" 
               data-status="<%= isOverdue ? 'overdue' : 'active' %>"
               data-pending="<%= pendingCount > 0 ? 'pending' : 'complete' %>">
            <input type="checkbox" id="assignment-mobile-<%= assignment.id %>-<%= idx %>" />
            <div class="collapse-title p-4">
              <div class="flex items-start gap-3">
                <div class="avatar placeholder flex-shrink-0">
                  <div class="<%= isOverdue ? 'stat-card-gradient-error' : 'stat-card-gradient-success' %> rounded-lg w-12 h-12 flex items-center justify-center text-white">
                    <i class="fas fa-clipboard-list text-lg"></i>
                  </div>
                </div>
                <div class="flex-1 min-w-0">
                  <div class="font-bold text-sm truncate"><%= assignment.title %></div>
                  <div class="flex flex-wrap items-center gap-2 mt-1">
                    <span class="badge badge-ghost badge-sm"><%= assignment.course.code %></span>
                    <% if (isOverdue) { %>
                      <span class="badge badge-error badge-xs">Overdue</span>
                    <% } else { %>
                      <span class="badge badge-success badge-xs">Active</span>
                    <% } %>
                  </div>
                  <div class="text-xs text-base-content/60 mt-1">
                    <i class="fas fa-calendar-alt mr-1"></i>
                    Due: <%= new Date(assignment.deadline).toLocaleDateString('en-US', { 
                      month: 'short', day: 'numeric', year: 'numeric',
                      hour: '2-digit', minute: '2-digit'
                    }) %>
                  </div>
                </div>
                <div class="flex flex-col items-end gap-1 flex-shrink-0">
                  <div class="flex items-center gap-1 text-sm">
                    <i class="fas fa-file-alt text-info text-xs"></i>
                    <span class="font-semibold"><%= submissionCount %></span>
                  </div>
                  <% if (submissionCount > 0 && pendingCount > 0) { %>
                    <span class="badge badge-warning badge-xs"><%= pendingCount %> pending</span>
                  <% } else if (submissionCount > 0 && pendingCount === 0) { %>
                    <span class="badge badge-success badge-xs">All graded</span>
                  <% } %>
                </div>
              </div>
            </div>
            <div class="collapse-content px-4 pb-4">
              <div class="pt-2 space-y-3">
                <!-- Course Info -->
                <div class="flex justify-between items-start text-sm">
                  <span class="text-base-content/70">Course:</span>
                  <a href="/teacher/courses/<%= assignment.course.id %>" class="link link-hover text-right">
                    <%= assignment.course.title %>
                  </a>
                </div>

                <!-- Description -->
                <% if (assignment.description) { %>
                  <div class="text-sm">
                    <span class="text-base-content/70">Description:</span>
                    <p class="mt-1 text-base-content/80"><%= assignment.description %></p>
                  </div>
                <% } %>

                <!-- Materials -->
                <% if (materialCount > 0) { %>
                  <div class="flex justify-between items-center text-sm">
                    <span class="text-base-content/70">Materials:</span>
                    <span class="badge badge-info badge-sm">
                      <i class="fas fa-paperclip mr-1"></i><%= materialCount %>
                    </span>
                  </div>
                <% } %>

                <!-- Grading Progress -->
                <% if (submissionCount > 0) { %>
                  <div class="text-sm">
                    <div class="flex justify-between items-center mb-1">
                      <span class="text-base-content/70">Grading:</span>
                      <span class="text-xs"><%= gradedCount %>/<%= submissionCount %></span>
                    </div>
                    <progress class="progress progress-warning w-full" value="<%= gradedCount %>" max="<%= submissionCount %>"></progress>
                  </div>
                <% } %>
                
                <!-- Actions -->
                <div class="flex gap-2 pt-2">
                  <a href="/teacher/assignments/<%= assignment.id %>/submissions" 
                     class="btn-global-primary btn-sm flex-1" style="border-radius: 9999px;">
                    <i class="fas fa-eye mr-2"></i>View
                  </a>
                  <a href="/teacher/assignments/<%= assignment.id %>/edit" 
                     class="btn-global-primary btn-sm flex-1" style="border-radius: 9999px;">
                    <i class="fas fa-edit mr-2"></i>Edit
                  </a>
                  <button onclick="deleteAssignment('<%= assignment.id %>', '<%= safeTitle %>')" 
                          class="btn-global-danger btn-sm flex-1" style="border-radius: 9999px; background-color: #ef4444; border-color: #ef4444; color: white;">
                    <i class="fas fa-trash mr-2"></i>Delete
                  </button>
                </div>
              </div>
            </div>
          </div>
        <% }); %>
      </div>
    <% } %>
  </div>

  <%- include('../shared/footer') %>

  <script>
    // Initialize DataTable
    let table;
    $(document).ready(function() {
      table = $('#assignmentsTable').DataTable({
        responsive: true,
        order: [[2, 'desc']], // Sort by deadline descending
        pageLength: 10,
        lengthMenu: [[10, 25, 50, -1], [10, 25, 50, "All"]],
        language: {
          search: "_INPUT_",
          searchPlaceholder: "Search assignments...",
          lengthMenu: "Show _MENU_ entries"
        },
        dom: '<"flex flex-wrap justify-between items-center mb-4"lf>rt<"flex flex-wrap justify-between items-center mt-4"ip>',
        columnDefs: [
          { orderable: false, targets: [6] } // Disable sorting on Actions column
        ]
      });

      // Course filter
      $('#courseFilter').on('change', function() {
        applyFilters();
      });

      // Status filter
      $('#statusFilter').on('change', function() {
        applyFilters();
      });

      // Grading filter
      $('#gradingFilter').on('change', function() {
        applyFilters();
      });
    });

    function applyFilters() {
      const courseId = $('#courseFilter').val();
      const status = $('#statusFilter').val();
      const grading = $('#gradingFilter').val();

      // Filter desktop table
      $.fn.dataTable.ext.search.pop(); // Remove previous filter
      $.fn.dataTable.ext.search.push(function(settings, data, dataIndex) {
        const row = table.row(dataIndex).node();
        const rowCourseId = $(row).data('course-id').toString();
        const rowStatus = $(row).data('status');
        const rowPending = $(row).data('pending');

        // Course filter
        if (courseId && rowCourseId !== courseId) {
          return false;
        }

        // Status filter
        if (status && rowStatus !== status) {
          return false;
        }

        // Grading filter
        if (grading && rowPending !== grading) {
          return false;
        }

        return true;
      });

      table.draw();

      // Filter mobile cards
      const mobileCards = document.querySelectorAll('.lg\\:hidden .collapse');
      mobileCards.forEach(card => {
        const cardCourseId = card.dataset.courseId;
        const cardStatus = card.dataset.status;
        const cardPending = card.dataset.pending;

        let show = true;

        // Course filter
        if (courseId && cardCourseId !== courseId) {
          show = false;
        }

        // Status filter
        if (status && cardStatus !== status) {
          show = false;
        }

        // Grading filter
        if (grading && cardPending !== grading) {
          show = false;
        }

        card.style.display = show ? '' : 'none';
      });
    }

    function resetFilters() {
      $('#courseFilter').val('');
      $('#statusFilter').val('');
      $('#gradingFilter').val('');
      $.fn.dataTable.ext.search.pop();
      table.draw();
      
      // Reset mobile cards
      const mobileCards = document.querySelectorAll('.lg\\:hidden .collapse');
      mobileCards.forEach(card => {
        card.style.display = '';
      });
    }

    /**
     * Delete assignment with confirmation
     */
    async function deleteAssignment(assignmentId, assignmentTitle) {
      // Set modal content
      document.getElementById('deleteModalTitle').textContent = `"${assignmentTitle}"`;
      document.getElementById('deleteAssignmentId').value = assignmentId;
      
      // Show modal
      document.getElementById('deleteConfirmModal').showModal();
    }

    /**
     * Confirm delete action
     */
    async function confirmDelete() {
      const assignmentId = document.getElementById('deleteAssignmentId').value;
      const modal = document.getElementById('deleteConfirmModal');
      const deleteBtn = document.getElementById('confirmDeleteBtn');
      
      // Show loading state
      deleteBtn.disabled = true;
      deleteBtn.innerHTML = '<span class="loading loading-spinner"></span> Deleting...';

      try {
        const response = await fetch(`/teacher/assignments/${assignmentId}`, {
          method: 'DELETE',
          headers: {
            'Content-Type': 'application/json'
          }
        });

        const result = await response.json();

        if (result.success) {
          // Close modal and reload
          modal.close();
          window.location.reload();
        } else {
          // Show error in modal
          showToast('Error: ' + result.message, 'error');
          deleteBtn.disabled = false;
          deleteBtn.innerHTML = '<i class="fas fa-trash mr-2"></i>Delete Assignment';
        }
      } catch (error) {
        console.error('Delete error:', error);
        showToast('Error deleting assignment. Please try again.', 'error');
        deleteBtn.disabled = false;
        deleteBtn.innerHTML = '<i class="fas fa-trash mr-2"></i>Delete Assignment';
      }
    }

    /**
     * Show toast notification
     */
    function showToast(message, type = 'info') {
      const toast = document.createElement('div');
      toast.className = `alert alert-${type} fixed top-4 right-4 w-auto max-w-md shadow-lg z-50`;
      toast.innerHTML = `
        <i class="fas fa-${type === 'error' ? 'exclamation-circle' : type === 'success' ? 'check-circle' : 'info-circle'}"></i>
        <span>${message}</span>
      `;
      document.body.appendChild(toast);
      
      setTimeout(() => {
        toast.style.opacity = '0';
        toast.style.transition = 'opacity 0.3s';
        setTimeout(() => toast.remove(), 300);
      }, 3000);
    }
  </script>

  <!-- Delete Confirmation Modal -->
  <dialog id="deleteConfirmModal" class="modal modal-bottom sm:modal-middle">
    <div class="modal-box">
      <h3 class="font-bold text-lg mb-4">
        <i class="fas fa-exclamation-triangle text-error mr-2"></i>
        Delete Assignment
      </h3>
      
      <p class="mb-4">
        Are you sure you want to delete <strong id="deleteModalTitle"></strong>?
      </p>
      
      <div class="bg-error/10 border border-error/20 rounded-lg p-4 mb-6">
        <p class="font-semibold text-error mb-2">
          <i class="fas fa-warning mr-2"></i>This will permanently delete:
        </p>
        <ul class="list-disc list-inside text-sm space-y-1 text-base-content/80">
          <li>The assignment</li>
          <li>All assignment materials</li>
          <li>All student submissions</li>
          <li>All grades for this assignment</li>
        </ul>
        <p class="text-sm font-semibold text-error mt-3">
          <i class="fas fa-ban mr-2"></i>This action cannot be undone.
        </p>
      </div>
      
      <input type="hidden" id="deleteAssignmentId">
      
      <div class="modal-action">
        <form method="dialog" class="flex gap-2">
          <button type="button" class="btn btn-ghost" onclick="document.getElementById('deleteConfirmModal').close()">
            <i class="fas fa-times mr-2"></i>Cancel
          </button>
          <button type="button" id="confirmDeleteBtn" class="btn-global-danger" onclick="confirmDelete()">
            <i class="fas fa-trash mr-2"></i>Delete Assignment
          </button>
        </form>
      </div>
    </div>
    <form method="dialog" class="modal-backdrop">
      <button>close</button>
    </form>
  </dialog>
</body>
</html>
