
# Application
NODE_ENV=development
PORT=3000

# Database Configuration
# Local development example: mysql://root:password@localhost:3306/lms_edumanage
# Railway production: DATABASE_URL is auto-injected by Railway MySQL plugin
DATABASE_URL=mysql://root:password@localhost:3306/lms_edumanage

# Session Secret (REQUIRED - minimum 32 characters for security)
# Generate a random string: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=your-super-secret-session-key-must-be-at-least-32-characters-long

# Cloudinary Configuration (REQUIRED for file uploads)
# Sign up at https://cloudinary.com and get your credentials from the dashboard
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret

# Notes:
# - Never commit the actual .env file to version control
# - SESSION_SECRET should be a long, random string (32+ characters)
# - For Railway deployment, set these as environment variables in the Railway dashboard
# - DATABASE_URL is automatically provided by Railway's MySQL plugin
